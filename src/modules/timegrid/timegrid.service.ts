import { Injectable } from '@nestjs/common';
import { UpdateTimeGridDto } from './dto/update-timegrid.dto';

@Injectable()
export class TimeGridService {

  // create(createTimeGridDto: any) {
  //   return TimeGrid.create(createTimeGridDto);
  // }

  // findAll() {
  //   return TimeGrid.findAll();
  // }

  // findOne(id: number) {
  //   return TimeGrid.findOne({ where: { id } });
  // }

  // update(id: number, updateScheduleDto: UpdateTimeGridDto) {
  //   return TimeGrid.update(updateScheduleDto, { where: { id } });
  // }

  // remove(id: number) {
  //   return TimeGrid.destroy({ where: { id } });
  // }
}
